import React from 'react';
import { Route } from 'react-router';
 
export default (
    // Switch is added in v4 react-router
    <Route>
        <Route path='/' />
        <Route path='/services' />
        <Route path='/contact' />
        <Route path='/blog' />
    </Route>
);